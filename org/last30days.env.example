# last30days / last30days-official 全局配置
# 复制到：  ~/.config/last30days/.env
# 权限：   chmod 600 ~/.config/last30days/.env
# OpenClaw：本机已用  ~/.openclaw/.env  →  指向本文件（符号链接），网关会按官方说明加载；
#           openclaw.json 根级 env.shellEnv.enabled=true，可从登录 shell 补变量。
# 诊断：   cd ~/.openclaw/workspace/skills/last30days-official && python3 scripts/last30days.py --diagnose
# Bird：   若 diagnose 里 bird_authenticated 仍为 false，在技能目录执行一次：
#          cd scripts/lib/vendor/bird-search && npm install
#          （ClawHub 同步的 skill 需本地装 @steipete/sweet-cookie；仓库已含 query-ids.json / features.json）
#
# ── 推荐最小集（国内 + 多源）────────────────────────────────────────
# 1) SCRAPECREATORS_API_KEY  — Reddit / TikTok / Instagram（v2.9+ Reddit 默认）
#    获取：https://scrapecreators.com 注册 → Dashboard → API Key
#
# 2) X（Twitter）二选一：
#    A. AUTH_TOKEN + CT0（推荐，本地 GraphQL，无 xAI 费用）
#       - 浏览器登录 https://x.com → F12 → Application → Cookies → x.com
#       - 复制 cookie 名 auth_token、ct0 的值（勿泄露、勿提交仓库）
#    B. 或只配 XAI_API_KEY（走 xAI 的 x_search）
#       - https://console.x.ai 等创建 API Key
#
# ── 可选增强 ────────────────────────────────────────────────────────
# OpenAI：旧版 Reddit 兜底；若仅 ScrapeCreators 可留空先试 diagnose
# OPENAI_API_KEY=sk-...
#
# Bluesky（技能 opt-in）
# BSKY_HANDLE=you.bsky.social
# BSKY_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx   # bsky.app → Settings → App Passwords
#
# Open 变体 / 自带网页检索（按需）
# PARALLEL_API_KEY=...
# BRAVE_API_KEY=...    # https://brave.com/search/api/
# OPENROUTER_API_KEY=...

# ── 下面填空后保存 ────────────────────────────────────────────────────
SCRAPECREATORS_API_KEY=

# X 方案 A：
AUTH_TOKEN=
CT0=
# X 方案 B（与 A 二选一即可，可都配优先 A）：
# XAI_API_KEY=

# OPENAI_API_KEY=
# BSKY_HANDLE=
# BSKY_APP_PASSWORD=
# PARALLEL_API_KEY=
# BRAVE_API_KEY=
# OPENROUTER_API_KEY=
